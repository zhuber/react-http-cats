import React, { PropTypes } from 'react';

class HttpCat extends React.Component {

  static propTypes = {
    status: PropTypes.number,
  }

  render() {
    return (
      <img src={`https://http.cat/${this.props.status}`} />
    )
  }
}

export default HttpCat;
